batch_path: ${paths.data_root}/batch
onnx_path: ${paths.artifacts_path}/onnx_models/children_drawings.onnx
parity:
  category:
    max_probability_diff: 1e-3
    check_argmax: true
  age:
    max_abs_error: 1e-2
  gender:
    max_probability_diff: 1e-3
    check_argmax: true
input_mode: random
seed: 42
